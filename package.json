{
  "name": "ai-optimize-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "AI Optimize - Configuration Compiler and Governance System",
  "packageManager": "pnpm@9.1.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "daemon": "pnpm --filter @ai-optimize/daemon dev",
    "cli": "pnpm --filter @ai-optimize/cli dev",
    "dashboard": "pnpm --filter @ai-optimize/dashboard dev"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
